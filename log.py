import customtkinter as ctk
from tkinter import messagebox

app = ctk.CTk()
app.title("waste log")
app.geometry("1000x600")

title = ctk.CTkLabel(app, text="waste logs", font=("Arial", 20))
title.pack(pady=10)

pastentry

app.mainloop()